# Code Review Fixes - Issue #56

## Summary

แก้ไขปัญหาที่พบจาก Luma Code Review สำหรับ Android Add Transaction feature

## Changes Made

### 1. Input Validation & Crash Prevention
- **`AddTransactionScreen.kt`**: เพิ่ม regex validation ป้องกันการป้อนจำนวนเงินผิดรูปแบบ (เช่น "1.2.3")
- **`AddTransactionScreen.kt`**: ใช้ `toDoubleOrNull()` แทน `toDouble()` ป้องกัน crash

### 2. Logic Error Fix
- **`Constants.kt`**: เพิ่ม `UNKNOWN_JAR` และ `UNKNOWN_WALLET` สำหรับ fallback เมื่อหา ID ไม่เจอ

### 3. Dead Code Removal
- **`DashboardScreen.kt`**: ลบ `FloatingBottomNavigation` และ `NavIcon` ที่ไม่ได้ใช้

### 4. Database Migration Testing
- **`MigrationTest.kt`**: สร้าง instrumented test สำหรับ MIGRATION_2_3
- **`AppDatabase.kt`**: เปิด `exportSchema = true`
- **`build.gradle.kts`**: Config KSP สำหรับ schema export

### 5. Test Configuration
- **`run_tests.sh`**: อัพเดทให้รัน unit tests ทั้งหมด

## Verification

```bash
./scripts/run_tests.sh
# BUILD SUCCESSFUL - 53 unit tests passed
```

## Files Modified

| File | Change |
|------|--------|
| `AddTransactionScreen.kt` | Input validation |
| `Constants.kt` | Unknown fallbacks |
| `DashboardScreen.kt` | Dead code removal |
| `AppDatabase.kt` | exportSchema=true |
| `MigrationTest.kt` | New migration test |
| `build.gradle.kts` | KSP schema config |
| `run_tests.sh` | Run all unit tests |

## Schema Files (commit these)

```
app/schemas/com.oatrice.jarwise.data.AppDatabase/
├── 2.json  # Manual - for migration testing
└── 3.json  # Auto-generated by Room
```

## Notes

- Migration tests อยู่ใน `androidTest` ต้องรันบน emulator/device:
  ```bash
  ./gradlew connectedDebugAndroidTest --tests "*.MigrationTest"
  ```
